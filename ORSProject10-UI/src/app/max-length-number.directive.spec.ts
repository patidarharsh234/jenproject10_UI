import { MaxLengthNumberDirective } from './max-length-number.directive';

describe('MaxLengthNumberDirective', () => {
  it('should create an instance', () => {
    const directive = new MaxLengthNumberDirective();
    expect(directive).toBeTruthy();
  });
});
